\newcommand*{\veeI}[1]{\RightLabel{$\vee$\lr I}\UnaryInfC{#1}}
\newcommand*{\veeE}[2]{\RightLabel{$\vee$\lr{E} $(#1)$}\TrinaryInfC{#2}}

\newcommand*{\botI}{\RightLabel{$\bot$\lr I}\BinaryInfC{$\bot$}}
\newcommand*{\botE}[1]{\RightLabel{$\bot$\lr E}\UnaryInfC{#1}}

%\newcommand*{\negI}[1]{\RightLabel{$\neg$\lr I}\UnaryInfC{#1}}
%\newcommand*{\negID}[2]{\RightLabel{$\neg$\lr I $(#1)$}\UnaryInfC{#2}}
\NewDocumentCommand{\negI}{o m}{%
  \IfNoValueTF{#1}{%
    % Clause for when the optional argument is not present
    \RightLabel{$\neg$\lr I}\UnaryInfC{#2}
  }{%
    % Clause for when the optional argument is present
    \RightLabel{$\neg$\lr I $(#1)$}\UnaryInfC{#2}
  }%
}

\newcommand*{\conI}[1]{\RightLabel{$\wedge$\lr I}\BinaryInfC{#1}}
\newcommand*{\conE}[1]{\RightLabel{$\wedge$\lr E}\UnaryInfC{#1}}

%\newcommand*{\toI}[2]{\RightLabel{$\to$\lr I $(#1)$}\UnaryInfC{#2}}
\NewDocumentCommand{\toI}{o m}{%
  \IfNoValueTF{#1}{%
    % Clause for when the optional argument is not present
    \RightLabel{$\to$\lr I}\UnaryInfC{#2}
  }{%
    % Clause for when the optional argument is present
    \RightLabel{$\to$\lr I $(#1)$}\UnaryInfC{#2}
  }%
}
\newcommand*{\toE}[1]{\RightLabel{$\to$\lr{E}}\BinaryInfC{#1}}

\newcommand*{\prop}{\textsc{\lr{Prop}}}

\NewDocumentCommand{\RAA}{o m}{%
  \IfNoValueTF{#1}{%
    % Clause for when the optional argument is not present
    \RightLabel{\lr{RAA}}\UnaryInfC{#2}
  }{%
    % Clause for when the optional argument is present
    \RightLabel{\lr{RAA} $(#1)$}\UnaryInfC{#2}
  }%
}

\newcommand*{\vdashIPC}{\vdash_\text{\lr{IPC}}}
\newcommand*{\vdashCPC}{\vdash_\text{\lr{CPC}}}