~\marginpar[left]{\textbf{(۲۰ نمره)}}
در دستگاه استنتاج طبیعی اثبات کنید
\begin{enumerate}
  \item $\neg (p \wedge q) \vdash \neg p \vee \neg q$
  \item $p \vee q \vdash \neg (\neg p \wedge \neg q)$
\end{enumerate}